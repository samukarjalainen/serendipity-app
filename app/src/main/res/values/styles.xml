<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Style for Activities -->
    <style name="BasicViewStyle">
        <item name="android:background">@color/bgPrimary</item>
        <item name="android:paddingLeft">@dimen/activity_horizontal_margin</item>
        <item name="android:paddingRight">@dimen/activity_horizontal_margin</item>
        <item name="android:paddingTop">@dimen/activity_vertical_margin</item>
        <item name="android:paddingBottom">@dimen/activity_vertical_margin</item>
    </style>

    <!-- EditText Style -->
    <style name="EditTextCustom">
        <item name="colorControlNormal">@color/hintTextColor</item>
        <item name="colorControlActivated">@color/mainTextColor</item>
        <!--<item name="android:textColorHighlight">#ff7eff15</item>-->
        <!--<item name="android:textColorHint">@color/hintTextColor</item>-->
    </style>

    <!-- Small button style -->
    <style name="BasicButton">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/mainTextColor</item>
        <item name="android:textSize">20sp</item>
    </style>

    <!-- Large button style -->
    <style name="LargeButton">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/mainTextColor</item>
        <item name="android:textSize">50sp</item>
    </style>

</resources>
